runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_KEY: base64:55Yl9OrR+ioz4sVZ3mPJ1+3FGhmNBLohRrmVbyNstO0=
  STORAGE_DIR: /tmp
  CACHE_DRIVER: database
  SESSION_DRIVER: database
  ## Set these environment variables according to your CloudSQL configuration.
  DB_HOST: localhost
  DB_DATABASE: islam_app_base
  DB_USERNAME: abbo
  DB_PASSWORD: zerty2018
  DB_SOCKET: "/cloudsql/proxima-cm:us-central1:islam-app"

beta_settings:
    # for Cloud SQL, set this value to the Cloud SQL connection name,
    # e.g. "project:region:cloudsql-instance"
    cloud_sql_instances: "proxima-cm:us-central1:islam-app"